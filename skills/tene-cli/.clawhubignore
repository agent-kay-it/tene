# ClawHub publish exclusions for the tene-cli skill bundle.
# Belt-and-suspenders: none of these should exist in skills/tene-cli/ anyway.

# Tene vault data (encrypted — still excluded)
.tene/
*.tene.enc

# Plaintext secrets
.env
.env.*
*.key
*.pem
*.p12
*.pfx

# Build artifacts
node_modules/
dist/
build/
tmp/
.next/
coverage/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Git internals (CLI also honors .gitignore)
.git/
